'use strict';

/**
 * Prime the `project` object, which will contain information
 * about the project being created.
 *
 * In other words:
 *  - generator => "from"
 *  - project => "to"
 *
 */

module.exports = function project_() {
  if (!this.has('project')) {
    this.set('project', {});
  }

  // if (this.)
};
