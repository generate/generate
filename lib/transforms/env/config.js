'use strict';

var Store = require('data-store');

/**
 * Create a local config store for storing
 * project-specific values.
 */

module.exports = function config_() {
  // var config = this.has('args.config') ||

  // if (this.enabled('local config store')) {
  //   this.config = new Store('.generate.json', {
  //     cwd: this.cwd
  //   });
  // }
};
